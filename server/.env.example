# MongoDB
MONGO_URI=mongodb+srv://<user>:<password>@cluster.mongodb.net/ecocredit?retryWrites=true&w=majority

# JWT (You can generate these using 'node -e "console.log(require(\"crypto\").randomBytes(32).toString(\"hex\"))"')
JWT_SECRET=your-jwt-secret-here
JWT_REFRESH_SECRET=your-refresh-secret-here

# Groq AI
GROQ_API_KEY=gsk_...

# Blockchain (optional)
RPC_URL=https://sepolia.infura.io/v3/YOUR_KEY
PRIVATE_KEY=your-wallet-private-key
CONTRACT_ADDRESS=0x...

# App
PORT=5000
CLIENT_URL=http://localhost:3000
NODE_ENV=development
